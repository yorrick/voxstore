# Optional: Anthropic API key for Claude Agent SDK (pay-as-you-go billing)
# If not set, the SDK uses the authenticated Claude Code session (Max subscription)
# ANTHROPIC_API_KEY=

# Required: Sentry webhook secret (client secret from Sentry internal integration)
SENTRY_WEBHOOK_SECRET=

# Required: Sentry API configuration
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# Optional: Sentry API base URL (default: https://sentry.io/api/0)
# SENTRY_API_URL=https://sentry.io/api/0

# Optional: GitHub PAT (falls back to `gh auth login`)
# GITHUB_PAT=

# Optional: Server port (default: 8002)
# PORT=8002

# Path to the voxstore repo root (for the agents to work in)
REPO_PATH=/home/yorrick/voxstore

# Required: Render deployment URL (used to trigger test errors, e.g. /sentry-debug)
RENDER_APP_URL=
